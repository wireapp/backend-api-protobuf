{
  "dependencies": {
    "protobufjs": "6.10.2"
  },
  "devDependencies": {
    "@types/node": "~12",
    "typescript": "latest"
  },
  "description": "Protocol definition for the backend API.",
  "files": [
    "proto/otr.proto",
    "web/Protobuf.d.ts",
    "web/Protobuf.js"
  ],
  "license": "GPL-3.0",
  "main": "web/Protobuf.js",
  "name": "@wireapp/backend-protocol-messaging",
  "repository": "https://github.com/wireapp/backend-api-protobuf.git",
  "scripts": {
    "dist": "node web/compile.js",
    "postversion": "git push origin && git push origin --tags && npm publish",
    "preversion": "git checkout master && git pull && yarn && yarn dist && yarn test",
    "test": "tsc --noEmit"
  },
  "types": "web/Protobuf.d.ts",
  "version": "2.3.0"
}
